<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Webpage</title>
    <style type="text/css">
/* Reset default browser margins and paddings */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Scroll Snap properties for the root element */
html {
    scroll-snap-type: y proximity; /* Changed from mandatory */
    overflow-y: scroll; /* Ensure vertical scrollbar is present for snapping */
    scroll-padding-top: 70px; /* Offset for fixed navigation bar */
}

/* Set a modern sans-serif font for the body */
body {
    font-family: 'Arial', sans-serif;
    color: var(--neutral-text-color);
    background-color: var(--neutral-background-color);
    line-height: 1.6; /* Improved readability */
}

/* Define a basic color palette */
:root {
    --primary-color: #007bff; /* Example primary color */
    --secondary-color: #6c757d; /* Example secondary color */
    --neutral-text-color: #333;
    --neutral-background-color: #f4f4f4;
    --header-bg: #343a40;
    --footer-bg: #343a40;
    --hero-bg-gradient: linear-gradient(135deg, #6e8efb, #a777e3); /* More distinct hero background */
    --content-cards-bg: #ffffff;
    --card-hover-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
    --card-hover-transform: scale(1.02);
}

/* Navigation Styles */
.main-nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white */
    backdrop-filter: blur(5px); /* Frosted glass effect */
    z-index: 1000; /* Ensure it's above other content */
    transition: transform 0.3s ease-out;
    box-shadow: 0 2px 5px rgba(0,0,0,0.05); /* Softer shadow */
}

.main-nav.nav-hidden {
    transform: translateY(-100%);
}

.main-nav.nav-scrolled { /* Style for when page is scrolled a bit, slightly more opaque */
    background-color: rgba(255, 255, 255, 0.95);
    box-shadow: 0 3px 8px rgba(0,0,0,0.08);
}

.nav-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 15px 20px; /* Adjusted padding */
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.6em; /* Slightly smaller logo */
    font-weight: bold;
    text-decoration: none;
    color: #333;
}

.nav-links {
    list-style: none;
    display: flex; /* For desktop view */
    margin: 0;
    padding: 0;
}

.nav-links li {
    margin-left: 30px; /* Spacing between links */
}

.nav-links a {
    text-decoration: none;
    color: #444; /* Slightly darker link color */
    font-weight: 500;
    font-size: 0.95em; /* Modest size */
    padding: 8px 0; /* Padding for clickable area and underline room */
    position: relative;
    transition: color 0.3s ease;
}

.nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0; /* Underline effect at the bottom of padding */
    left: 0;
    width: 0;
    height: 2px;
    background-color: var(--primary-color, #333); /* Use primary color or fallback */
    transition: width 0.3s ease;
}

.nav-links a:hover {
    color: #111; /* Darker on hover */
}

.nav-links a:hover::after {
    width: 100%;
}

.nav-toggle {
    display: none; /* Hidden on desktop */
    background: none;
    border: none;
    cursor: pointer;
    padding: 10px;
    z-index: 1001; /* Ensure toggle is above nav-links in mobile potentially */
}

.hamburger-bar {
    display: block;
    width: 22px; /* Slightly smaller hamburger */
    height: 2px; /* Thinner bars */
    background-color: #333;
    margin: 5px 0;
    transition: transform 0.3s ease, opacity 0.3s ease;
}

.main-nav.nav-open .hamburger-bar:nth-child(1) {
    transform: translateY(7px) rotate(45deg); /* Adjusted translate based on 2px bar height and 5px margin */
}

.main-nav.nav-open .hamburger-bar:nth-child(2) {
    opacity: 0;
}

.main-nav.nav-open .hamburger-bar:nth-child(3) {
    transform: translateY(-7px) rotate(-45deg); /* Adjusted translate */
}

/* Responsive Navigation for Smaller Screens */
@media (max-width: 768px) {
    .nav-container {
        padding: 10px 15px; /* Adjust padding for smaller nav bar */
    }

    .nav-toggle {
        display: block; /* Show hamburger toggle */
    }

    .nav-links {
        display: none; /* Hide desktop links by default */
        flex-direction: column;
        position: absolute;
        top: 100%; /* Position below the nav bar (adjust if nav bar height changes) */
        left: 0;
        width: 100%;
        background-color: #ffffff; /* Solid white background for mobile menu */
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        padding: 0; /* Reset padding */
        border-top: 1px solid #eee; /* Separator */
    }

    .main-nav.nav-open .nav-links { /* Show mobile menu when nav-open class is present */
        display: flex;
    }

    .nav-links li {
        margin: 0; /* Reset margin */
        width: 100%;
    }

    .nav-links a {
        display: block; /* Make links take full width */
        padding: 15px 20px; /* Larger touch targets */
        text-align: left; /* Align text to the left */
        border-bottom: 1px solid #f0f0f0; /* Separators between links */
    }

    .nav-links a::after { /* Remove underline hover effect for mobile nav items */
        display: none;
    }

    .nav-links li:last-child a {
        border-bottom: none;
    }
}

/* General container for max-width and centering */
.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px; /* Add some padding to prevent content from touching edges */
}

/* Style the header */
header {
    background-color: var(--header-bg);
    color: white;
    padding: 20px 0; /* Adjust padding for container */
}

header .container {
    display: flex;
    justify-content: center; /* Center content within header */
    align-items: center;
}

header p {
    font-size: 1.2em; /* Style header placeholder text */
}

/* Style the main section */
main {
    /* Main itself does not need padding if sections have it */
}

/* Style the hero section */
#hero {
    background-image: var(--hero-bg-gradient);
    color: white; /* Text color on gradient */
    min-height: 60vh;
    padding: 40px 20px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
}

#hero p {
    font-size: 2em; /* Larger, more prominent text */
    font-weight: bold;
    max-width: 80%; /* Prevent text from being too wide */
}

/* Style the content-cards section */
#content-cards {
    background-color: var(--content-cards-bg);
    padding: 40px 0; /* Adjust padding for container */
    scroll-snap-align: center; /* Snap point */
    scroll-snap-stop: always; /* Prevent skipping this snap point */
}

#content-cards .container {
    display: grid; /* Changed from flex to grid */
    grid-template-columns: repeat(12, 1fr); /* 12 columns for fine-grained control */
    gap: 20px; /* Add space between cards */
}


/* Style individual cards */
.card {
    background-color: #ffffff; /* White background for cards */
    padding: 25px; /* Increased padding */
    border: 1px solid #ddd; /* Light grey border */
    border-radius: 15px; /* Rounded corners */
    box-shadow: 0 8px 16px rgba(0,0,0,0.15); /* More prominent box shadow */
    /* flex-basis and min-width removed for grid layout */
    box-sizing: border-box; /* Include padding and border in the element's total width and height */
    grid-column: span 4; /* Default span for a 3-across feel */
    min-height: 200px; /* Give cards some substance */

    /* Initial state for scroll-triggered animation */
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out, box-shadow 0.3s ease-in-out; /* Ensure opacity and transform are part of transition */
}

/* Asymmetrical card styling */
.card:nth-child(1) { grid-column: span 6; }
.card:nth-child(2) { grid-column: span 6; }
.card:nth-child(3) { grid-column: span 3; }
.card:nth-child(4) { grid-column: span 5; }
.card:nth-child(5) { grid-column: span 4; } /* This was default, but explicit here */
.card:nth-child(6) { grid-column: span 12; } /* Full width */
.card:nth-child(7) { grid-column: span 5; }
.card:nth-child(8) { grid-column: span 7; }
.card:nth-child(9) { grid-column: span 6; }
.card:nth-child(10) { grid-column: span 6; }
.card:nth-child(11) { grid-column: span 12; } /* Full width */
/* Card 12 will take default span 4 */


.card:hover {
    transform: var(--card-hover-transform); /* var(--card-hover-transform) is scale(1.02) */
    box-shadow: 0 12px 24px rgba(0,0,0,0.2); /* More pronounced shadow on hover */
}

.card h3 {
    color: var(--primary-color);
    margin-bottom: 15px; /* Slightly more space */
    font-size: 1.5em;
    font-weight: bold; /* Ensure headings are distinct */
}

.card p {
    font-size: 1em;
    line-height: 1.6; /* Ensure consistent line height */
    color: var(--neutral-text-color);
    text-align: left; /* Default, but good to be explicit for card content */
}

/* Styles for #our-founders section (formerly grouped with #company-history) */
#our-founders {
    height: 100vh;
    display: flex;
    align-items: center;
    scroll-snap-align: start; /* Changed from center to start */
    scroll-snap-stop: always;
    justify-content: center;
    text-align: center;
    padding: 20px;
    color: #ffffff; /* White text */
    position: relative; /* For overlay positioning if needed, though background directly on section works */
    background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://images.unsplash.com/photo-1556761175-5973dc0f32e7?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1932&q=80') no-repeat center center/cover;
}

/* Fallback background for #our-founders if Unsplash URLs are problematic - uncomment to use
#our-founders {
    background: linear-gradient(135deg, #3a5372, #4b6b8c);
}
*/

.section-content-wrapper {
    background-color: rgba(0, 0, 0, 0.6); /* Slightly darker overlay for better readability */
    padding: 40px;
    border-radius: 10px;
    max-width: 800px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Optional: add a subtle shadow to the wrapper */
    /* Initial state for scroll-triggered animation */
    opacity: 0;
    transform: translateY(50px); /* Or scale(0.95) */
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}

/* .is-visible class to trigger animation */
.is-visible {
    opacity: 1;
    transform: translateY(0); /* Or scale(1) if scale was used initially */
}

#company-history h2,
#our-founders h2 {
    font-size: 3em;
    margin-bottom: 20px;
    font-weight: bold; /* Ensure it stands out */
}

#company-history p,
#our-founders p {
    font-size: 1.2em;
    line-height: 1.7;
}

/* Rebuilt Company Story Card Styles */
.story-card-rebuilt {
    background-color: #ffffff;
    border-radius: 45px; /* Very rounded corners */
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1); /* Softer, deeper shadow */
    padding: 60px 70px; /* Large internal padding */
    margin: 0 auto; /* Center the card within the .container if .container is wider */
    max-width: 950px; /* Max width for the card */
    text-align: left; /* Reset text-align from parent section's center */
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.7s ease-out, transform 0.7s ease-out, box-shadow 0.3s ease-out; /* Added box-shadow to transition */
}

.story-content-rebuilt h2 {
    font-size: 2.8em;
    color: var(--primary-color, #333);
    margin-bottom: 30px;
    text-align: center; /* Center the heading within the card */
    font-weight: 600;
}

.story-content-rebuilt p {
    font-size: 1.25em;
    line-height: 1.85;
    color: #343a40;
    text-align: justify; /* Justified text for the paragraph */
}

/* Responsive adjustments for the rebuilt story card padding on smaller screens */
@media (max-width: 768px) {
    .story-card-rebuilt {
        padding: 40px 30px;
    }
    .story-content-rebuilt h2 {
        font-size: 2.2em;
    }
    .story-content-rebuilt p {
        font-size: 1.1em;
    }
}

/* Scroll Snap for Rebuilt Company Story */
#company-story-rebuilt {
    scroll-snap-align: start;
    scroll-snap-stop: always;
}

/* Styles for the new Products section */
#products {
    padding: 60px 0; /* Increased padding for more space */
    /* background-color: #f9f9f9; */ /* Optional: a slightly different background for this section */
    scroll-snap-align: center; /* Make this section a snap point */
    scroll-snap-stop: always; /* Prevent skipping this snap point */
}

.product-card {
    display: flex;
    align-items: center;
    background-color: #ffffff;
    border-radius: 40px; /* Very rounded corners */
    box-shadow: 0 10px 25px rgba(0,0,0,0.08); /* Subtle shadow */
    margin-bottom: 40px; /* Increased spacing between cards */
    overflow: hidden; /* Ensures child elements conform to border-radius */
    /* Initial state for scroll-triggered animation */
    opacity: 0;
    transform: translateY(50px);
    transition: transform 0.3s ease-out, box-shadow 0.3s ease-out, opacity 0.3s ease-out; /* Updated transition */
    cursor: pointer; /* Added cursor pointer */
}

.product-card:hover {
    transform: scale(1.03) translateY(-5px); /* Slightly raise and scale up */
    box-shadow: 0 20px 40px rgba(0,0,0,0.15); /* More pronounced shadow */
}

/* Apply .is-visible to .product-card as well for consistency */
.product-card.is-visible {
    opacity: 1;
    transform: translateY(0);
}


.product-card .product-text {
    flex: 3; /* Give more space to text */
    padding: 40px; /* Generous padding */
}

.product-card .product-text h3 {
    font-size: 2.2em; /* Larger title */
    margin-bottom: 15px;
    color: var(--primary-color); /* Use existing primary color variable */
}

.product-card .product-text p {
    font-size: 1.1em;
    line-height: 1.7;
    color: #555; /* Slightly darker paragraph text */
}

.product-card .product-image {
    flex: 2; /* Give less space to image compared to text */
    padding: 20px;
    display: flex; /* To center image if it's smaller than container */
    align-items: center;
    justify-content: center;
}

.product-card .product-image img {
    width: 100%;
    max-width: 350px; /* Max width for the image to control its size */
    height: auto;
    display: block;
    border-radius: 25px; /* Rounded corners for the image itself */
    object-fit: cover; /* Ensures image covers the area well */
}

/* Alternating layout for product cards */
.product-card:nth-child(even) .product-text {
    order: 2; /* Moves text to the right for even cards */
}

.product-card:nth-child(even) .product-image {
    order: 1; /* Moves image to the left for even cards */
}

/* Style the footer */
footer {
    background-color: #2c3e50; /* Dark background for footer */
    padding: 20px 0; /* Padding for the main footer element */
    color: #bdc3c7; /* Default light text for footer, e.g., copyright */
    border-top: 3px solid var(--primary-color); /* Keep existing border */
    scroll-snap-align: end;    /* Snap to the end of the footer to ensure all its content is visible */
    scroll-snap-stop: always;
    min-height: 40vh;          /* Give it some snap-worthy vertical space, adjust as needed */
    display: flex;             /* To help center its content if footer is made tall */
    align-items: center;       /* Vertically center .container */
    justify-content: center;   /* Horizontally center .container */
}

/* Footer Content Styles */
.footer-content {
    text-align: center;
    padding: 30px 0; /* Increased padding */
    color: #f0f0f0; /* Assuming a dark footer background, light text */
}

.footer-content h4 {
    font-size: 1.6em; /* Slightly larger heading */
    margin-bottom: 20px;
    color: #ffffff; /* White heading on dark footer */
    font-weight: 600;
}

.footer-content address { /* Reset browser default address styling */
    font-style: normal;
}

.footer-content address p {
    margin-bottom: 10px;
    font-size: 0.95em; /* Slightly smaller for secondary info */
    line-height: 1.6;
}

.footer-content address a {
    color: #e0e0e0; /* Lighter link color for readability on dark bg */
    text-decoration: none;
    transition: color 0.3s ease;
}

.footer-content address a:hover {
    color: #ffffff; /* Brighter on hover */
    text-decoration: underline;
}

.social-links {
    margin-top: 25px; /* More space above social links */
}

.social-links p {
    margin-bottom: 12px; /* Space between "Follow Us:" and links */
    font-size: 1.1em; /* Slightly larger "Follow Us" */
    font-weight: 500;
    color: #ffffff;
}

.social-links a {
    color: #f0f0f0;
    text-decoration: none;
    margin: 0 12px; /* More spacing between social icons */
    font-weight: 500; /* Normal weight for FB, IG, TW text */
    font-size: 1em;
    padding: 5px 8px; /* Padding for touch targets / visual balance */
    border: 1px solid transparent; /* For spacing and potential hover effect */
    border-radius: 5px; /* Slightly rounded hover effect */
    transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
}

.social-links a:hover {
    color: #111; /* Dark text on hover */
    background-color: #ffffff; /* White background on hover */
    border-color: #ffffff; /* White border on hover */
}


footer .container {
    display: flex; /* This was for the old simple p tags, might need adjustment for new structure */
    flex-direction: column; /* Stack footer-content and copyright */
    align-items: center; /* Center content */
}

footer p { /* This will target the copyright paragraph */
    font-size: 0.9em;
    margin-top: 20px; /* Space above copyright */
    text-align: center; /* Ensure copyright is centered */
}

/* If the copyright is separate and needs centering (specific selector from prompt) */
footer .container > p[align="center"] {
    margin-top: 20px;
    font-size: 0.9em;
}

/* Company History Timeline Section */
#company-history {
    padding: 60px 0;
    background-color: var(--neutral-background-color, #f4f4f4); /* Use existing variable or a light neutral color */
    scroll-snap-align: start; /* Snap point */
    scroll-snap-stop: always;
    overflow: hidden; /* To contain floats or absolute positioning if used in timeline items */
}

#company-history h2 {
    text-align: center;
    font-size: 2.5em; /* Consistent with other section titles */
    color: var(--neutral-text-color, #333);
    margin-bottom: 50px;
}

.timeline {
    position: relative;
    max-width: 800px; /* Adjust as needed */
    margin: 0 auto;
    padding: 20px 0;
}

/* The timeline axis (New) */
.timeline-line {
    position: absolute;
    width: 4px; /* Thickness of the timeline */
    background-color: var(--primary-color, #007bff);
    top: 0;
    /* bottom: 0; */ /* Removed to allow height animation from 0 to 100% */
    left: 50%;
    margin-left: -2px; /* Center the line */
    z-index: 0; /* Ensure it's behind dots and content */
    height: 0; /* Initially hidden, to be animated by JS */
    transition: height 1s ease-out; /* Animation transition */
}

.timeline-item {
    padding: 10px 40px;
    position: relative;
    background-color: inherit;
    width: 50%;
    box-sizing: border-box;
}

/* The timeline dot */
.timeline-item .timeline-dot {
    content: '';
    position: absolute;
    width: 16px;
    height: 16px;
    background-color: #fff;
    border: 4px solid var(--primary-color, #007bff);
    top: 28px; /* Adjust to align with content */
    border-radius: 50%;
    z-index: 1;
}

/* Position items on the left and right */
.timeline-item:nth-child(odd) {
    left: 0;
    padding-left: 0; /* Adjust padding for items on the left */
}

.timeline-item:nth-child(even) {
    left: 50%;
    padding-right: 0; /* Adjust padding for items on the right */
}

/* Dot positioning for left and right items */
.timeline-item:nth-child(odd) .timeline-dot {
    right: -8px; /* Half of dot width to align on the line */
}

.timeline-item:nth-child(even) .timeline-dot {
    left: -8px; /* Half of dot width to align on the line */
}

/* Date styling */
.timeline-date {
    position: absolute;
    top: 26px; /* Align with the dot */
    font-weight: bold;
    color: var(--primary-color, #007bff);
    font-size: 1.1em;
    z-index: 1; /* Ensure date is above the line if it overlaps */
}

.timeline-item:nth-child(odd) .timeline-date {
    right: 60px; /* Position date to the left of the dot for odd items */
    text-align: right;
}

.timeline-item:nth-child(even) .timeline-date {
    left: 60px; /* Position date to the right of the dot for even items */
}

/* Content card styling */
.timeline-content {
    padding: 20px 30px;
    background-color: var(--content-cards-bg, #ffffff); /* Use existing card background */
    position: relative;
    border-radius: 15px; /* Consistent with other cards */
    box-shadow: 0 8px 16px rgba(0,0,0,0.15); /* Use existing shadow */
    /* Match card class from existing styles for consistency if possible */
}

.timeline-content h3 {
    font-size: 1.5em;
    color: var(--primary-color, #333);
    margin-bottom: 10px;
}

.timeline-content p {
    font-size: 1em;
    line-height: 1.6;
    color: var(--neutral-text-color);
}

/* Add some animation to timeline cards, similar to other cards */
.timeline-item .timeline-content {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out, box-shadow 0.3s ease-in-out;
}

/* .is-visible class will be added by JS, assuming similar JS observer is used for these elements */
.timeline-item .timeline-content.is-visible {
    opacity: 1;
    transform: translateY(0);
}

/* Hide old timeline-date elements, if any remain or for safety */
.timeline-date {
    display: none !important;
}

/* Styling for the new dates on the timeline axis */
.timeline-dot {
    /* Ensure the dot itself is a positioning context if needed, though not strictly necessary if axis-date is positioned relative to timeline-item */
}

.timeline-axis-date {
    position: absolute;
    width: 80px; /* Width of the date box */
    text-align: center;
    padding: 5px 0;
    background-color: var(--content-cards-bg, #ffffff); /* Match card background */
    color: var(--primary-color, #007bff);
    font-weight: bold;
    font-size: 0.9em;
    border: 1px solid var(--primary-color, #007bff);
    border-radius: 4px;
    z-index: 2; /* Above the timeline line and dot's main circle */

    /* Positioning: Center it on the dot initially, then adjust */
    top: 50%; /* Vertically center relative to dot */
    transform: translateY(-50%); /* Precise vertical centering */
}

/* Adjust date position based on whether item is on left or right */
.timeline-item:nth-child(odd) .timeline-axis-date {
    left: 30px; /* Place it to the right of the timeline axis/dot */
}

.timeline-item:nth-child(even) .timeline-axis-date {
    right: 30px; /* Place it to the left of the timeline axis/dot */
}

/* Responsive adjustments for timeline */
@media screen and (max-width: 768px) {
    .timeline-line { /* Updated from .timeline::after */
        left: 31px; /* Move timeline axis to the left for mobile */
    }

    .timeline-item {
        width: 100%;
        padding-left: 70px; /* Space for date and dot */
        padding-right: 25px;
    }

    .timeline-item:nth-child(odd),
    .timeline-item:nth-child(even) {
        left: 0%; /* All items flow on one side */
        padding-left: 70px; /* Ensure consistent padding */
        padding-right: 15px; /* Adjust right padding */
    }

    .timeline-item .timeline-dot {
        left: 23px; /* Position dot on the left axis */
        /* right: auto; remove right positioning */
    }

    .timeline-item:nth-child(odd) .timeline-dot,
    .timeline-item:nth-child(even) .timeline-dot {
        left: 23px; /* Ensure dot is always on the left axis */
        right: auto;
    }

    .timeline-date {
        position: relative; /* Change positioning for mobile */
        top: auto;
        left: auto !important; /* Override inline/specific positioning */
        right: auto !important;
        text-align: left !important; /* Align date text to left */
        margin-bottom: 10px; /* Space between date and content */
        display: block; /* Make it block to take its own line */
        font-size: 1em;
        color: var(--secondary-color, #6c757d); /* Maybe a more subtle color for date on mobile */
    }

    /* Adjust content card for mobile */
    .timeline-content {
        margin-left: 0; /* Reset margin if any was added for desktop */
    }

    /* Ensure date is displayed correctly for all items on mobile */
    .timeline-item:nth-child(odd) .timeline-date,
    .timeline-item:nth-child(even) .timeline-date {
       /* All mobile date adjustments are handled by .timeline-date general rules above for mobile */
    }

    /* Responsive adjustments for dates on axis */
    .timeline-axis-date {
        /* On mobile, dates are on the left, before the content */
        left: -85px !important; /* Move it further left of the dot on the single-sided mobile layout */
        right: auto !important; /* Ensure no interference from 'even' rule */
        width: auto; /* Allow it to size naturally or set a specific mobile width */
        padding: 3px 8px;
        font-size: 0.85em;
    }

    .timeline-item .timeline-axis-date { /* More specific selector for mobile */
        left: -65px;
        position: absolute;
        transform: translateY(-50%); /* Keep vertical centering */
        left: -60px; /* Adjusted for better spacing from the timeline line */
    }
}

/* Future is Unwritten Indicator */
.timeline-future-indicator {
    position: relative;
    width: 50%; /* Align with one side of the timeline items */
    padding: 20px 40px;
    box-sizing: border-box;
    margin-left: 50%; /* Position on the right side of the timeline */
    text-align: center; /* Center the spans if they were inline-block */
}

.timeline-future-indicator span {
    display: block;
    width: 4px; /* Match timeline axis thickness */
    height: 20px; /* Length of each dash */
    background-color: var(--primary-color, #007bff);
    margin: 10px auto; /* Space out the dashes, centered */
    opacity: 0.7;
}

.timeline-future-indicator span:nth-child(2) {
    opacity: 0.5;
    transform: scaleY(0.8); /* Make middle dash slightly shorter */
}

.timeline-future-indicator span:nth-child(3) {
    opacity: 0.3;
    transform: scaleY(0.6); /* Make last dash even shorter */
}

/* Responsive adjustments for the indicator */
@media screen and (max-width: 768px) {
    .timeline-future-indicator {
        width: 100%;
        margin-left: 0; /* Full width on mobile */
        padding-left: 70px; /* Align with mobile timeline items' content */
        padding-right: 25px;
        text-align: left; /* Align dashes to the left with content */
    }
    .timeline-future-indicator span {
        margin: 10px 0; /* Align left, not auto for center */
    }
}
    </style>
</head>